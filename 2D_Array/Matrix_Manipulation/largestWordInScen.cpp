#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
using namespace std;

int main()
{
    string s;
    int n;
    cout<<"Enter n: ";
    cin>>n;
    char arr[n+1];
    cin.getline(arr,n);
    vector<int> v;
    int st = 0; // temp for size.
    while(st<n){
        
        count++;
        if(arr[st] ==" " || arr[st] == "\0"){
            

        }
    }




    return 0;
}